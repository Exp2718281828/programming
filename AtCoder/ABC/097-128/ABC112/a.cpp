#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(0);
	ios::sync_with_stdio(false);
	int n, a, b;
	cin >> n;
	if (n == 1)
		cout << "Hello World" << endl;
	else {
		cin >> a >> b;
		cout << a + b << endl;
	}
}