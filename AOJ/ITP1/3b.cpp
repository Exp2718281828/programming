#include <cstdio>
#include <iostream>
using namespace std;
int main() {
    int i = 0, x;
    while (0 == 0) {
        i++;
        cin >> x;
        if (x == 0) break;
        printf("Case %d: %d\n", i, x);
    }
}